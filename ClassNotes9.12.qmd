---
title: "ClassNotes9.12"
author: "Adam Kaderbhai"
format: html
---

```{r}
#| message: false
#| label: setup

library(tidyverse)
```

## Subsetting data sets

We Subset rows in a data set using the `filter` command, which loads with `tidyverse`. Let's see this using `penguins`.

```{r}
glimpse(penguins)
View(penguins)
```

A set with just adelies:

```{r}
filter(penguins,
       species == "Adelie")
```

We can also filter numerically:
```{r}
penguins_sm <- filter(penguins,
                      body_mass > 3600)
glimpse(penguins_sm)
```


Two conditions at once:
```{r}
penguins_sm <- filter(penguins,
                      body_mass > 3600,
                      species == "Adelie")
glimpse(penguins_sm)
```

One specie **OR** another:
```{r}
penguins_sm <- filter(penguins,
                      species == "Adelie" | 
                        species == "Chinstrap")
glimpse(penguins_sm)

```
